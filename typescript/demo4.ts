// type annotation 类型注解 鼠标放变量上，弹出的东西就是类型注解
// type inference 类型推断
// 如果

/* let countFour: number;
countFour = 123;

let countInference = 123; */

/* const one = 1;
const two = 2;
const three = one + two; */

function getTotal(one: number, two: number) {
  return one + two;
}

const total = getTotal(1, 2);

const XiaoJieJieFour = {
  name: "刘英",
  age: 18,
};
 